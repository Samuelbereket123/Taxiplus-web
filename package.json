{
  "name": "taxiplus-website",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/prebuild.js",
    "build": "npm run prebuild && nuxt build",
    "dev": "nuxt dev",
    "generate": "npm run prebuild && nuxt build",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && node scripts/prebuild.js"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.2"
  }
}